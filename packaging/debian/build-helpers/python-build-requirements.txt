# Build-time Python stack for the DEB build. Matches the FlagOS NVIDIA DEB
# ABI matrix (FEP-0019 / flagos-ai/community#59):
#   Ubuntu 22.04 + CUDA 12.8 + Python 3.12 + PyTorch 2.10 + Triton 3.6 + pybind11 3.0
# torch/triton come from the FlagOS index (see TORCH_INDEX in Dockerfile.deb).
# A 22.04 build also installs on 24.04 (glibc/libstdc++ are backward compatible;
# libpython3.12t64 provides libpython3.12 on amd64); the reverse does not hold.
torch==2.10.0+cu128
triton==3.6.0
pybind11==3.0.3
