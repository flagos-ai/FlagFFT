# 合并 1d-test 并适配 adaptor

## 时间日期

- 2026-05-27 19:12:03 CST (Asia/Shanghai)

## 用户指令

- 将 `1d-test` 合并到 `main`。
- 使用当前 adaptor 形式修改 `1d-test` 中的测试，仅调整 adaptor 所需部分，不修改测试逻辑。

## 工作计划

- 在 `1d-test` 合入当前 `main`，以 `main` 的 `src/adaptor/` 结构解决冲突。
- 保留 1D 参数化测试、确定性数据、精度判定和 smoke/full 运行策略。
- 将旧参考计划及裸设备资源操作改接 `RefPlanHandle` 和 `flagfft::adaptor::Memory`。
- 构建并运行 smoke 与完整 CTest，通过后将已验证分支合回 `main`。

## 落实产物

- `ctest` 测试通过 `flagfft::test_adaptor::RefPlanHandle` 使用共享参考 FFT adaptor。
- 测试设备缓冲切换为 `flagfft::adaptor::Memory` RAII 管理，保留原测试执行与比较流程。
- `ctest/flagfft_test.h` 仅保留测试辅助逻辑，旧 `ctest/backend/` 实现不再引入。
- `README.md` 与 1D 测试计划文档更新为当前 adaptor 架构及 smoke/full 使用方式。
- 重新安装当前工作区的 `flagfft-codegen` Python 包，修复测试运行环境原先指向已不存在工作区的问题。

## 测试结果

- `cmake --build build`：通过。
- 首次 `FLAGFFT_TEST_PROFILE=smoke ctest --test-dir build --output-on-failure`：失败，原因为 `flagfft_codegen.jit_source` 无法导入；确认属于运行环境依赖，不是测试逻辑或 adaptor 编译问题。
- `python3 -m pip install .` 后完成最终复验：`FLAGFFT_TEST_PROFILE=smoke ctest --test-dir build --output-on-failure` 通过，9/9 测试目标通过（109.20 秒）。
- 最终 `ctest --test-dir build --output-on-failure`：通过，9/9 测试目标通过（268.62 秒）。
